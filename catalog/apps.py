"""Django app config for catalog."""

from __future__ import unicode_literals

from django.apps import AppConfig


class CatalogConfig(AppConfig):
    """Django config for catalog module."""
    name = 'catalog'
